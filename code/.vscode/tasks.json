{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install requirements",
            "type": "shell",
            "command": "${command:python.interpreterPath} -m pip install --upgrade --upgrade-strategy eager -r requirements.txt",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}